# Ride_List
A C file that prints the rides that the user inputs. Prompts the user for the amount and the names.

# How to run
Navigate to the file usinf cd until ls gives you a list that contains the file.
Then run gcc ride_list.c, then run ./a.out.
Then input the amount of rides.
Then the name of each ride.
